query Fridges($limit: Int!, $cursor: String) {
  fridges(cursor: $cursor, limit: $limit) {
    hasMore
    fridges {
      id
      createdAt
      updatedAt
      title
      textSnippet
    }
  }
}
